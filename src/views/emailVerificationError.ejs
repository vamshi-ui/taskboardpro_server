<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification Failed</title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        font-family: Arial, sans-serif;
        background: #ffebee;
      }
      .container {
        text-align: center;
        background: #fff;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      h1 {
        color: #d32f2f;
      }
      p {
        margin: 10px 0;
      }
      .btn {
        display: inline-block;
        padding: 10px 20px;
        color: #fff;
        background: #d32f2f;
        text-decoration: none;
        border-radius: 5px;
        transition: background 0.3s;
      }
      .btn:hover {
        background: #b71c1c;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Email Verification Failed</h1>
      <p><%= message %></p>
      <p>
       your token has expired, please try registering again or request a new
        verification email.
      </p>
      <a href="<%= process.env.CLIENT_URL %>/signup" class="btn"
        >Go to Registration</a
      >
    </div>
  </body>
</html>
